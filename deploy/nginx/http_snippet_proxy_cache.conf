# Раскомментируйте и включите из основного nginx.conf (внутри http { }):
#
# proxy_cache_path /var/cache/nginx/prod-encar levels=1:2 keys_zone=prod_encar_api:20m max_size=2g inactive=30m use_temp_path=off;
#
# Затем в server { } замените или дополните location /api/ так:
#
# location /api/ {
#     proxy_cache prod_encar_api;
#     proxy_cache_key "$scheme$request_method$host$request_uri";
#     proxy_cache_valid 200 30s;
#     proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
#     proxy_cache_background_update on;
#     proxy_cache_lock on;
#     add_header X-Cache-Status $upstream_cache_status;
#     proxy_pass http://127.0.0.1:8080/api/;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
# }
#
# Учитывайте персональные эндпоинты: для /api/me, /api/favorites и т.д. кэш обычно отключают отдельным location.
