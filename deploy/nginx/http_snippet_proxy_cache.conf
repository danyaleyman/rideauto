# --- Включить ОДИН РАЗ в http { } основного nginx.conf (например /etc/nginx/nginx.conf) ---
#
# proxy_cache_path /var/cache/nginx/prod-encar levels=1:2 keys_zone=prod_encar_api:20m max_size=2g inactive=30m use_temp_path=off;
#
# Первый запуск (права под пользователя nginx — обычно www-data):
#   sudo mkdir -p /var/cache/nginx/prod-encar
#   sudo chown -R www-data:www-data /var/cache/nginx/prod-encar
#
# Сайт: используйте deploy/nginx/prod-encar.conf — там отдельные location для кэша и обхода.
# Nginx выбирает самый длинный подходящий префикс location — персональные /api/me, /api/favorites
# идут без proxy_cache, публичные /api/cars, /api/facets — с micro-cache.
