.git
**/__pycache__
*.pyc
.pytest_cache
.cursor
.github

# Host-side Next artifacts (large); image runs `npm ci` / `npm run build` itself
web/node_modules
web/.next

# Shrinks `docker compose build web` context (image copies `web/`, not whole monorepo)
deploy/
docs/
e2e/
infrastructure/
scripts/
playwright.config.mjs
Prod Encar.code-workspace
README.md
